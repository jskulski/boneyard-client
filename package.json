{
  "devDependencies": {
    "babel": "^5.6.5",
    "chai": "^3.0.0",
    "jsdom": "3.*",
    "jsx-loader": "^0.13.2",
    "mocha": "^2.2.5",
    "parallelshell": "^1.1.1",
    "sinon": "^1.15.3",
    "testdom": "^1.0.1",
    "webpack": "^1.9.12",
    "webpack-dev-server": "^1.9.0"
  },
  "engines": {
    "node": ">=0.12.0"
  },
  "private": true,
  "scripts": {
    "dist": "npm run-script clean && npm run-script build",
    "serve": "npm run-script dist& npm run-script start",
    "clean": "rm -rf dist",
    "build": "webpack -d -w --content-base build/",
    "start": "node server.js",
    "test": "mocha --compilers js:babel/register --recursive"
  },
  "dependencies": {
    "nanoajax": "^0.3.1",
    "node-static": "^0.7.6",
    "react": "^0.13.3"
  }
}
